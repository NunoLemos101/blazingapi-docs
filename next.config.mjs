export default {
    typescript: {
        ignoreBuildErrors: true, // This setting ignores TypeScript errors during the build process
    },
    eslint: {
        ignoreDuringBuilds: true, // Optionally ignore ESLint warnings/errors as well
    },
};